services:
  - type: web
    name: fundspark-ai-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    autoDeploy: false
